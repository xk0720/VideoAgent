
import os
import numpy as np

for _ in range(NotImplemented):

    # e.g., scene 2 shot 1 at the moment
    scene_shot_id = NotImplemented

    # agent make decision...




###

import torch.nn as nn

emotion_bank = nn.Embedding(num_embeddings=NotImplemented, embedding_dim=NotImplemented)

#