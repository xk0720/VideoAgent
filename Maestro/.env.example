# v0.1 runs fully on mocks — no keys needed.
# Fill these in v0.2 when swapping mocks for real models.

# LLM (planning brains)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
DEEPSEEK_API_KEY=

# MLLM / VLM judge & critic
QWEN_API_KEY=

# Video generation backend (local weights paths or API key)
# Used by src/maestro/models/video_gen_backends.py
OMNIWEAVING_WEIGHTS=     # local path to OmniWeaving checkpoint
WAN_WEIGHTS=             # local path to Wan2.x checkpoint
VEO_API_KEY=            # hosted API fallback (Veo / Sora)
