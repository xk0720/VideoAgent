# Core (always required; mock pipeline + tests + server run with just these)
numpy>=1.23,<3.0
pyyaml>=6.0,<7.0

# Test / dev
pytest>=7.0,<9.0

# Server (optional — only needed if you run `python -m maestro.cli serve`)
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.5,<3.0

# Image ops (optional — image_ops degrades gracefully if missing)
pillow>=10.0,<12.0

# ── Real-backend extras (optional; NOT needed for the CPU mock pipeline) ──
# Video frame decoding for the real C6 track extractor (cotracker/tapir).
# opencv-python-headless>=4.8,<5.0
# imageio[ffmpeg]>=2.30,<3.0
#
# Heavy GPU model backends are deliberately NOT pinned (match them to your CUDA):
#   torch                      # required by any real model backend
#   # cotracker                # C6 real point tracking (models.track_extractor.name: cotracker)
#   # the OmniWeaving / Wan / Veo SDK for real video generation
#   # the V-JEPA-2 weights for models.world_reward.name: vjepa2
# Install these yourself, then flip the matching `name` in configs/*.yaml.
